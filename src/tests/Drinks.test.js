import { renderWithRouter } from './helpers/renderWith';
import App from '../App';

describe('', () => {
  beforeEach(() => {
    renderWithRouter(<App />, { initialEntries: ['/drinks'] });
  });

  it('', () => {
  });
});
